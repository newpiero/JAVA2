package Circles;


import java.awt.*;
import java.util.Timer;
import java.util.TimerTask;

public class ColorCanvas {

    private final Color colorCan = new Color(
            (int)(Math.random() * 255),
            (int)(Math.random() * 255),
            (int)(Math.random() * 255)
    );
    Timer timer = new Timer();

    public static void selectColor () {

    }


}
